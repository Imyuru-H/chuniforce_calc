[project]
name = "chuniforce"
version = "0.2.0"
description = "Your project description"
requires-python = ">=3.14.3"
dependencies = [
    "fastapi",
    "jinja2",
    "aiohttp",
    "workers-py",
    "requests",
    "itsdangerous>=2.2.0",
    "sqlalchemy>=2.0.51",
    "aiosqlite>=0.22.1",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
